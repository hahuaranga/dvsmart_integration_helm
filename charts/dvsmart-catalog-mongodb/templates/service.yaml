apiVersion: v1
kind: Service
metadata:
  name: {{ include "dvsmart-catalog-mongodb.fullname" . }}
  labels:
    {{- include "dvsmart-catalog-mongodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dvsmart-catalog-mongodb.selectorLabels" . | nindent 4 }}
