apiVersion: v1
kind: Service
metadata:
  name: {{ include "dvsmart-reorganization-api.fullname" . }}
  labels:
    {{- include "dvsmart-reorganization-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dvsmart-reorganization-api.selectorLabels" . | nindent 4 }}
