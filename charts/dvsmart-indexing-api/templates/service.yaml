apiVersion: v1
kind: Service
metadata:
  name: {{ include "dvsmart-indexing-api.fullname" . }}
  labels:
    {{- include "dvsmart-indexing-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dvsmart-indexing-api.selectorLabels" . | nindent 4 }}
