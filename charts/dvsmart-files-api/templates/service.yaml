apiVersion: v1
kind: Service
metadata:
  name: {{ include "dvsmart-files-api.fullname" . }}
  labels:
    {{- include "dvsmart-files-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dvsmart-files-api.selectorLabels" . | nindent 4 }}
